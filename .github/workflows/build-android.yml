name: Build Android APK

on:
  push:
    branches:
      - main

jobs:
  build:
    name: Build APK
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout Source
        uses: actions/checkout@v3

      - name: Setup Java
        uses: actions/setup-java@v3
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      # 1. Install Dependencies (Safe & Clean)
      - name: Install Dependencies
        run: |
          npm install --legacy-peer-deps

      # 2. Build Web Assets
      - name: Build Web App
        run: npm run build
        env:
          CI: false

      # 3. Install Capacitor CLI globally for reliable execution
      - name: Install Capacitor CLI
        run: npm install -g @capacitor/cli@6.0.0

      # 4. Generate Android Project FRESH on Linux (Solves all OS mismatch issues)
      - name: Generate Android Project
        run: |
           rm -rf android
           cap add android
           cap sync android

      # 4. Upgrade Gradle to fix compatibility issues
      - name: Upgrade Gradle Wrapper
        working-directory: android
        run: |
          chmod +x gradlew
          ./gradlew wrapper --gradle-version=8.5

      # 5. Build APK with upgraded Gradle
      - name: Build APK
        working-directory: android
        run: |
          chmod +x gradlew
          ./gradlew assembleDebug --stacktrace

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: mouna-app-debug
          path: android/app/build/outputs/apk/debug/app-debug.apk
